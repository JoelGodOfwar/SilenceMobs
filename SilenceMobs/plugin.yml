name: SilenceMobs
main: com.github.joelgodofwar.sm.SilenceMobs
version: 1.10_1.0.2
author: JoelYahwehOfWar

commands:
  sm:
    description: Shows SilenceMobs help.
    usage: /<command>
  silencemobs:
    description: Shows SilenceMobs help.
    usage: /<command>

permissions:
  silencemobs.use:
    description: Allows player to silence mobs.
    default: true
  silencemobs.reload:
    description: Allows the player to use "/mmh reload" command.
    default: op
  silencemobs.toggledebug:
    description: Allows the player to use "/mmh toggledebug" command.
    default: op
  silencemobs.showUpdateAvailable:
    description: Shows player that there is a version update.
    default: op
